# セッション引き継ぎ_<MMDD>_<題>

親: [[現在地]] / 状態: **引き継ぎ（<YYYY-MM-DD>・<送り側マシン> → <受け側>）** / 出典チャット: `<チャット名>`（session `<id 先頭 8 桁>`）

> **受け側が最初にやること**: §1 の順に読む → §7 のデータを落とす → §8 の記憶を移植 → §9 の受領確認を書いてから §4 の一手に着手。

## 0. 一行
（このセッションで何が決まり、何が残ったか。3 行以内）

## 1. 読む順（vault の正本・全部読む）
1. [[現在地]] → [[引用禁止]] → [[運用ルール]]（3 点セット）
2. …（このセッションの結果ノート・spec を積んだ順に）

## 2. 今の絵（結論・棄却したもの・外れた予測）
## 3. 進行中・保留（pid / run id / 触ってよいか）
## 4. 次の一手（優先順・spec があるならリンク）
## 5. このセッションで踏んだ／踏みかけた線
## 6. 機械的な状態（`collect_state.sh` の出力を貼る）
## 7. データ（manifest・URL・展開先）
## 8. 記憶の移植（`memory/` の一覧と、受け側で読み替えるべきもの）
## 9. 受領確認の書式
## Log
