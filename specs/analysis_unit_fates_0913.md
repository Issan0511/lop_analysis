# 個別unitの成長と中心群の出入り：追加の事後解析 0913

親: spec_zero_attraction_learning_0913.md / 状態: 実装前に解析手順を固定。
前走の全19条件の終端結果は既知。分岐の因果・独立の予測検定とは呼ばない。新しい学習はしない。
ユーザーの問い: 育つ/育たない分かれ道は特定できたか。CondA ELUで0付近の群が単調に減るという観察も受領（減る量・元動画は未同定）。

対象: 既存の通常/peak/valley Snake q0、各10seed・100unit・task0..500。
- ID=(seed,unit)を保ち、task端点のみ使う。task内の離脱は未観測と明記。
- 状態M: |平均前活性-root|<=.1。状態A: 全32入力の前活性がroot±.1。閾値は先行specと同じ。±.05,.2も感度として保存。
- 各taskの在籍数、流入・流出、stayを数える。n(t)-n(t-1)=in-outを検算。個数の単調性と同じ個体の滞留は別物。
- 固定cohort task20および100時点のM/A内外について、task500の状態・初期比の自由重みnorm・v・出力分散を記述する。群を将来状態で選んだ結果は予測に使わない。
- task500の自由重みが初期未満の群/以上の群を事後に定義し、task0,1,5,20,100,500の各群のnorm/|v|/平均前活性絶対値を保存。これは違いがいつ見えるかの記述であり原因とは呼ばない。
- 発育境界の因果同定は行わない。勾配ノルム・既存方向射影・readout・meanの介入は別途必要。
- seed内集約→seed平均、各seed値も保存。1000unitを1000独立標本として有意差を出さない。
- source SHA、ID/step/shape/有限性、前解析の同じ閾値における最終値、流入流出恒等式を検算。

## 補助ELU軌道の照合（集計前追補）
ユーザーから「0付近の個数」と確認済み。元のアニメーション条件は未特定。
同じCondA系の参照として、既存p3_extend_0902 E_1216 seed0–9、ELU alpha1・1層・用量12.16・lr.01・1500taskを読む。
平均zの±.05,.1,.2を中心帯とし、task0,1,5,20,100,500,1500の個数と全隣接taskの流入出を測る。
初期20/100で帯内のIDを固定し、500/1500の帯内・mean z< -1・< -5、fullW（freeWはこのログに無い）とvの変化を保存。
これは別の既存条件での補助記述。動画と同じ走の再現、ELUの自由重みの収縮判定、Snakeとの活性化だけの因果比較とは呼ばない。
