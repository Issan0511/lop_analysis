# Retained input-invisible state: orth_reveal_0918 follow-up

Registered after the original 18 primary endpoints were read: pooled B/A visibility of the new w0-orthogonal A increment was negative in all six cells, positive-side selectivity unresolved, and Kdiff positive in the three a=.7 cells. This follow-up is explicitly secondary and does not replace those primary outcomes. Its new retained-state endpoints have not been computed.

Rationale: whole-task D-perpendicular-w0 differs from a component of the actual accumulated weight that is invisible to A inputs. The latter may be retained from earlier tasks even when A cannot add weight to its own input nullspace. Test it directly with the same saved arrays, without extra optimization or choosing a favorable cell.

For full weights only, let P_A project onto the span of all 32 Task A inputs (same relative SVD cutoff1e-10); N=I-P_A; n=w1*N at A end. Confirm n*x_A=0, n≈w0*N, and <n,w0>=||n||² up to actual A-rounding drift. Thus n is input-invisible but generally NOT perpendicular to w0. Keep this distinction explicit. All six cells, ten seeds, all fifteen B flips.

Report n norm fraction, before-B all-flip RMS(n*x_B), full input-jump RMS and the energy/cross decomposition of that jump into n and P_A*w1. Ratios of squared components are descriptive and not variance-explained claims when cross terms exist. Record both signs of new field and signed mean. No B/A RMS ratio: its denominator is identically zero.

For the same single-B and no-switch trajectories already saved, fix n, B support and qn=n*x_B at B start. Report whole-weight tagged coefficient loss K_n=-sum<n,delta_w>/sum||n||², and its difference against no-switch control. Actual delta_w_N=delta_w*N isolates changes to the A-invisible weight. Decompose changes to B response exactly into delta_w_N*x_B and delta_w_P*x_B. For fixed qn>0 and qn<0 receivers, Cn_±=-sum(qn * delta_w_N*x_B)/sum(qn²), plus total-W C± as a separate metric. Report sign-specific counteraction and switch-minus-control selectivity. Repeat fixed qn-sign × initial full-z_B-sign cells. Mean response changes from null and span channels must sum to actual W-only mean transport; bias separately.

Use all saved times for curves, end10000 for summaries. Six-cell seed means and descriptive95% t intervals, no new confirmatory labels or treating units/flips as independent. To flag multiplicity, additionally report 12-comparison adjusted intervals for K_n switch-control and Cn positive-minus-negative switch-control, labelled secondary follow-up. All metrics and provenance stored separately. Do not call retained null weights newly accumulated during A, and do not assert that this identifies their historical source or proves the original w-orthogonal growth mechanism.
