# relu_doors_0919 — ReLU が沈む道を 1 本ずつ塞ぐ

run id: `relu_doors_0919` / worktree `~/Projects/claude/wt/relu_doors_0919` / branch `claude/relu_doors_0919`
親: `rlcifar_mlp_battle_0918`（同じ箱・§8 の事後解析がこの spec の出所）、`pmnist_rlcifar_0907`（箱の定義）
起案: Issa「中心化だけの条件（多分これ中間層も絶対いるのと、あとバイアスに WD もいる）で ReLU 単独の効果みたいです」。
扉 H の実装（EMA 走行平均）と扉 B の範囲（**第 1 層にはかけず b₂ のみ**）は Issa が選んだ。

## 0. 一行

RL-CIFAR × MLP で ReLU は raw でも std でも崩壊する。崩壊は「前活性の中心が自分の広がりの 1.6 倍ぶん負へ行ってゲートが厳密に 0 になる」ことで起きる。
中心を押し下げる道は **(C) 入力の直流成分、(H) 中間層の入力の直流成分、(B) bias** の 3 本ある。**扉を 1 枚ずつ足す梯子（`ref` → `C` → `CH` → `CHB` → `CHB1`）で、ReLU が生き残るのに何枚要るかを決める。**

## 1. 出所（`rlcifar_mlp_battle_0918` §8。すべて事後・登録外だが、この spec の設計根拠）

- 沈みはほぼ全部 **W·(その層の入力の平均)** で、bias の取り分は第 1 層 3–13%、第 2 層 **0.03%**。
- ランダムな重みでは |z̄ᵢ|/sdᵢ ≈ r（r = ‖平均‖/rms‖偏差‖）なので、**初期化時の片側ユニットの割合 = 2Φ(−2.33/r)**。raw は r = 1.911±0.016 で実測 25.3%±2.5%（予測 22.3%）、std は r = 0.121 で 0.0%。10 seed で一致。膝は r ≈ 1。
- **raw の ReLU**: タスク 1 で第 1 層が全滅（死亡率 1.000）、online 0.109、‖w‖ は初期値から 1.00 倍＝1 ミリも動かない。
- **std の ReLU**: タスク 1 を 0.982 で覚えきってから、タスク 2 のラベル切り替えで第 2 層が落ちる（死亡率は 0.000 のまま、z̄₂/sd₂ が −0.59 → −1.33 → −1.57、ゲートが厳密 0 の割合 72% → 95% → 100%）。以後は勾配が厳密 0 で凍結。
- **第 2 層の入力 ā₁ の r は 0.49–0.68**（膝の下）。第 2 層の沈みは初期化でなく、**学習が W₂ を ā₁ の逆向きに揃える**結果。
- leaky が生き残るのは φ′ に 0.1 の床があるからで、**ゲートが厳密 0 になる割合は 50 タスク通して 0%**。

## 2. 箱

宿主 `src/pmnist_rlcifar_0907.py` を**無改変で import**する。3072-100-100-10、seed ごとに CIFAR-10 の 1200 枚を固定、タスクごとに一様乱数ラベル、50 タスク × 400 epoch × 75 step（batch 16）、Adam lr 1e−3（moment 持ち越し）、WD なし、正規化層なし、float32。
**入力は raw（u8/255）に固定**する。`rlcifar_mlp_battle_0918` の std 条件は「中心化と尺度を同時に変える」ので、ここでは尺度を動かさず中心化だけを扉にする。
エンジンは `rlcifar_mlp_battle_0918` の束ねエンジン（`baddbmm` + autograd + 要素ごと Adam + CUDA グラフ）を流用し、**1 プロセス = 1 腕 × seed 0–9（R = 10）**。

### 2.1 三つの扉

| 扉 | 定義 |
|---|---|
| **C** | 入力の中心化。x ← x − x̄、x̄ は**その seed の 1200 枚の平均**（走る前に一度だけ計算し、学習も評価も同じ x を使う）。**割らない**（尺度は raw のまま）。これで r が 1.911 → 0.121 になる。 |
| **H** | 中間層の中心化。a₁ ← a₁ − m₁、a₂ ← a₂ − m₂。mₗ はユニットごとの **EMA 走行平均**、mₗ ← (1−β)mₗ + β·mean_batch(aₗ)、**β = 0.01**（適応 α と同じ）、初期値 0。**mₗ は定数扱いで勾配を通さない。訓練と評価で同じ値を使う**（訓練/評価のずれを作らない）。更新は前向きの計算のあと、Adam の更新と同じ位置で行う。 |
| **B** | bias の封鎖。**b₂ にだけ** decoupled WD: Adam の更新のあとに b₂ ← b₂ − lr·λ·b₂ を毎 step。**b₁ と b₃ は自由**（Issa の指定）。λ は §2.2 で導出する。 |

### 2.2 λ の導出（固定値を置かない）

decoupled WD の平衡は「Adam の 1 step の歩幅 ≈ lr」と「減衰 lr·λ·b」の釣り合いで **|b|_eq ≈ 1/λ**。
崩壊の閾値は §1 の実測から **z̄₂/sd₂ = −1.6**。bias が単独でそこへ届かないよう **8 倍の余裕**を取り、目標を |b₂|_eq = 0.2·sd(z₂) と置く。

sd(z₂) は走る前に**未使用 seed 100–102 で `CH` 腕を 3 タスクだけ回して実測**し（`--probe`）、その 3 seed の t1–t3 の sd(z₂) の中央値 S を使って

$$\lambda = \frac{1}{0.2\,S}$$

と定める。得られた S と λ は走る前に spec の追補と `provenance.json` に記録する。**λ を決めたあとに動かさない。**

## 3. 腕（4 本の梯子）

| 腕 | C | H | B | 備考 |
|---|---|---|---|---|
| `ref` | − | − | − | **`rlcifar_mlp_battle_0918` の `R` raw を bit 一致で再利用**（新たに走らせない。S-reuse で確認）。既知: 窓 0.1132・t1 の死亡率 1.000・‖w‖ は初期値の 1.00 倍 |
| `C` | ✓ | − | − | |
| `CH` | ✓ | ✓ | − | λ の probe にも使う |
| `CHB` | ✓ | ✓ | ✓ | **主腕** |
| `CHB1` | ✓ | ✓ | ✓+b₁ | b₁ にも同じ λ の WD。`CHB` が b₁ 経由で崩れた場合の切り分け |

**新規 4 腕 × seed 0–9 = 40 走。**活性化は全腕 ReLU、入力は全腕 raw。
隣り合う腕の差が各扉の効果になる（`C`−`ref` = C、`CH`−`C` = H、`CHB`−`CH` = B、`CHB1`−`CHB` = b₁）。

### 3.1 落とした腕（開示）

2³ の残り 4 セル（`H` / `B` / `HB` / `CB`）は走らせない。理由は **raw の ReLU がタスク 1 で第 1 層ごと全滅している**（死亡率 1.000）ことで、扉 H も B も第 1 層の入力の直流成分（r = 1.911）に触らないため、入力を中心化しない腕は `ref` と同じ死に方をすると見込まれるから。
**落とす前に Claude が付けていた確率を記録しておく**（あとで「都合の悪い腕を落とした」と読まれないため）: `H` 崩壊 0.80、`B` 崩壊 0.95、`HB` 崩壊 0.85、`CB` は `C` とほぼ同じ（b₂ の現在の寄与が 0.03% のため）。
梯子の結果が上の見込みと食い違ったら、**そのときに該当セルを追加で走らせ、追補に記録する**。

## 4. 登録する読み出し

タスクごとに宿主の `evaluate_rl` の列に加えて:

- `online_acc`（更新前 argmax の平均）、`memo_acc`
- 両層の `zbar`・`zsd`・`dead_frac`、**`gate_zero_frac`**（φ′ が厳密に 0 の要素の割合）
- **`sink_ratio_l` = z̄ₗ / sd(zₗ)**（崩壊の直接の指標）
- **`bias_share_l` = |b̄ₗ| / |z̄ₗ|** と **`bias_over_sd_l` = |b̄ₗ| / sd(zₗ)**（扉が bias に移ったかを見る。**b₁ 側を必ず取る**）
- `w_norm_l1..3`、`r_a1`（第 2 層の入力の平均/偏差）

窓 = t31–50 の `online_acc` の平均、低下 = (t1–10) − (t41–50)、**崩壊 = 窓 < 0.5**、崩壊時刻 = `sink_ratio_l2` が初めて −1.6 を下回るタスク。
腕対腕は seed 対応の**厳密二項符号検定**、「勝ち」= 相手が勝つ seed が 1 以下かつ p < 0.05（0918 と同じ規則）。

## 5. 登録する判定

| 記号 | 判定 | ラベル |
|---|---|---|
| **M**（主） | `CHB` の窓 | `RESCUED`（10 seed 中 9 本以上が窓 ≥ 0.5）/ `SPLIT`（2–8 本）/ `COLLAPSED`（1 本以下） |
| **N** | 梯子上で最初に `RESCUED` になる腕 | `NEED_C` / `NEED_CH` / `NEED_CHB` / `NEED_CHB1` / `NONE_RESCUED` |
| **D1** | `C` − `ref` の窓 | `C_HELPS` / `TIE` / `C_HURTS` |
| **D2** | `CH` − `C` の窓 | `H_HELPS` / `TIE` / `H_HURTS` |
| **D3** | `CHB` − `CH` の窓 | `B_HELPS` / `TIE` / `B_HURTS` |
| **D4** | `CHB1` − `CHB` の窓 | `B1_HELPS` / `TIE` / `B1_HURTS` |
| **R1**（第 1 層の扉） | `C` の t1 の `dead_frac_l1` | `L1_SAVED`（< 0.10）/ `L1_DIES` |
| **R2**（第 2 層の扉） | `CH` の t50 の `sink_ratio_l2` | `L2_HELD`（> −1.6）/ `L2_SUNK` |
| **B_ROUTE** | `CH` の t50 の `bias_over_sd_l2` が `C` より大きいか（符号検定） | `BIAS_TAKES_OVER` / `BIAS_IRRELEVANT` |
| **B1_ROUTE** | `CHB` の t50 の `bias_over_sd_l1` が `C` より大きいか | `L1_BIAS_TAKES_OVER` / `L1_BIAS_IRRELEVANT` |

`ref` は既存走の再利用なので、D1 の比較だけ seed 対応が既存走との対応になる（同じ seed・同じ画像・同じラベル列）。

## 6. 予測

### Claude（2026-09-19 01:0x 記入、実装前・走る前）

| 記号 | 主張 | 確率 |
|---|---|---|
| P1 | **M = `RESCUED`**（`CHB` が救う） | 0.55 |
| P2 | `C` 単独は崩壊する（std の ReLU と同じ死に方） | 0.85 |
| P5 | `CH` は崩壊する（b₂ が扉を引き継ぐ） | 0.60 |
| P6 | **B_ROUTE = `BIAS_TAKES_OVER`** | 0.60 |
| P7 | R1 = `L1_SAVED`（`C` で t1 の第 1 層の死亡が 0.10 未満） | 0.80 |
| P8 | R2 = `L2_HELD`（`CH` で第 2 層が −1.6 より上を保つ） | 0.45 |
| P9 | **B1_ROUTE = `L1_BIAS_TAKES_OVER`**（b₂ を塞ぐと b₁ に移る） | 0.50 |
| P10 | D4 = `B1_HELPS`（`CHB1` が `CHB` に勝つ） | 0.40 |
| P11 | N = `NEED_CHB`（扉 3 枚で足りる） | 0.40 |
| P13 | `CHB` が救った場合、窓は leaky 0.1 の raw（0.726）を上回る | 0.35 |
| P14 | どの腕でも `gate_zero_frac_l2` が 1.00 に達した走は、以後 1 本も回復しない | 0.90 |

読み: **C は第 1 層の扉として必要だがそれだけでは足りず、H が第 2 層の扉を閉じ、そこで b₂ に移り、B で閉じると今度は b₁ が空いている**、というのが本線。`CHB1` まで要る可能性（P10・P11 の組み合わせ）を 0.4 と見る。§3.1 で落とした 4 腕に付けていた確率はそちらに記録した。

### Issa（**空欄**。走る前に記入し、時刻と「結果を読む前かどうか」を明記する）

- M:
- N:
- B_ROUTE:
- B1_ROUTE:

## 7. 検査（変異対照つき。空虚な検査を書かない）

| 記号 | 何を確かめるか | 変異対照（**落ちること**を確認する） |
|---|---|---|
| S-ref | `ref` が `rlcifar_mlp_battle_0918` の `R` raw と**行単位で bit 一致** | seed をずらすと落ちる |
| S-C | 扉 C の入力が x − x̄ で、x̄ が seed の 1200 枚の平均（‖x̄_new‖ < 1e−5） | 中心化を外すと落ちる |
| S-H | 扉 H の EMA が漸化式どおりで、**評価と訓練で同じ m を使う**。m を切ると z̄₂ が ā₁ の寄与ぶん動く | β を 0 にすると落ちる |
| S-B | b₂ の更新が「Adam の更新のあとに ×(1 − lr·λ)」で、b₁・b₃ が無傷 | λ を 0 にすると落ちる／b₁ に当たっていたら落ちる |
| S-off | 全部の扉を閉じた実装で、扉を全部 OFF にすると `ref` と bit 一致 | 扉を 1 つ ON にすると落ちる |
| S-grad | m を定数扱いしている（m 経由の勾配が 0） | 勾配を通すと落ちる |
| S-diverge | 発散したスロットだけ止まり、他の走に影響しない | — |
| S-resume | ckpt からの再開が bit 一致 | — |
| S-cost | R = 10 の 1 タスクの壁時計と並列数の最適点 | — |

閾値は**すべて実測から導く**（0918 の S-eval・S-reuse で 2 回踏んだ）。bit 一致を要求できない箇所は、宿主自身の実現値のばらつきで較正する。

## 8. 実行計画

1. `--probe` で seed 100–102 × `CH` を 3 タスク → S を実測 → λ を確定 → 追補に記録。
2. 検査 9 本を通す（`all_pass` を `results/_checks_relu_doors_0919/checks.json` に）。
3. Issa の予測欄を埋めてから本走。**4 腕 × 10 seed、1 プロセス 1 腕、並列 4**（Codex が `joudaki_vit_battle_0919` で同じ GPU を使っているので取り合いを避ける）、想定 6 時間。
4. 走の開始時に git 状態を `provenance.json` に取る。長い launch 中に `src/`・`analysis/` を触るときは STOP ファイルで起動を止めてから。
5. 完走後: report → figures → summary.md / verdict.json を commit → vault の結果ノート → §4 片付け（**`data/` symlink を辿らない**）。

## 9. 開示

- `rlcifar_mlp_battle_0918` の §8（事後解析）を全部読んだうえでこの spec を書いている。§1 に要点を写した。**§8 は登録外なので、ここでの主張の根拠としては「設計の動機」までに留め、結論には使わない。**
- `ref` は既存走の再利用で、その走の結果（窓 0.1132・死亡率 1.000）は既知。D1 の比較はこの既知の値に対するもの。
- 未使用 seed は 100 以上（登録の 0–9 を汚さない）。
- 2³ のうち 4 セルを走らせないことと、その判断に使った確率は §3.1 に開示した。
