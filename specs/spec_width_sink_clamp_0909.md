# 幅か沈下か クランプ介入 spec 0909
状態: 検証中 / 更新: 2026-09-09
親: [[駆動源問題_0909]] / [[上端の張り付き_事後_0909]] / [[幅と沈下の介入_結果_0909]]
run id: `width_sink_clamp_0909`

## 0. 何を閉じるか
[[駆動源問題_0909]] §5.3「幅との結合」と §6 条件 5（「幅が増える理由」と「幅があると下向きになる理由」を分ける）。
観測（[[上端の張り付き_事後_0909]]・事後）: 素 Adam の 4 活性化で、タスク終端に折れ目 0 より上にある前活性の割合 **p⁺ = P(z>0)** が 80 タスクにわたって ±10% で保たれ（leaky 0.16・ELU 0.20・Snake 0.29・WD 入りは 0.5）、その間に平均 z̄ は 2〜3 倍沈み、幅 σ は 1.7〜2.4 倍に伸びる。
**Issa の問い: 幅が先か、沈下が先か。** 既存の [[幅と沈下の介入_結果_0909]] は一発摂動で、しかも `star_i` を厳密保存するため p⁺ を動かしていない（介入直後の frac(z̄ᵢ>0) は κ 3 水準で同値）。自然な漂流の**駆動を止める**検定は未実施。本 spec は**連続クランプ**で一方を止め、他方が続くかを見る。新規学習軌道は作るが、保存済み task20 状態からの決定的再生のみ（GPU 走なし）。

## 1. 継続と腕
開始状態 = task20 終端（`results/boundary_tasks20_40_0909/source/{ARM}_{iv}_s{seed}_states.pt` の `boundaries[0]` の `before`・`adam_before`・SNA は V）。継続は **t21..t100（80 タスク × 625 更新）**。t21 は保存 `perm`、t22 以降は `boundaries[0]['rng_after_perm']` から生成器 3 本（perm/data/batch）を生かしたまま引く。この経路は `task20to100_0908` と同一なので、無介入継続は (i) t21..40 の保存 20 状態を params・Adam・V とも差 0、(ii) t100 の `results/boundary_groups_0908/{prefix}_task100.pt` を差 0、(iii) `results/task20to100_0908/{prefix}.npz` のタスク終端 81 点（zmean・star・within）を 1e−10 で再現しなければならない。

腕: **LR/none・SNA/none × seed0..2 × 介入 3 = 18 継続**（＋seed0 の変異対照）。l2 腕は沈まない（p⁺ 0.5・0.37）ので入れない。ELU は task20 の保存状態が無いので入れない（必要なら別 spec）。

介入は**毎更新、Adam 更新の直後**に適用する。Adam の m・v・t と Snake の V には触らない（クランプは最適化器の外側）。対象は第 1 層のみ。
- **`ref`**: 無介入。参照。
- **`wclamp`（幅クランプ）**: W1 の行 i を W_i = m_i·1 + W̃_i（行平均＋中心化）と書き、更新後に **W̃_i ← W̃_i · c_i / ‖W̃_i‖**、c_i = 開始時（t20 終端）の ‖W̃_i‖。m_i・b_i・W2・W3 は触らない。行和・star_i・z̄ の置換平均は不変（κ 腕と同じ性質）。除去した半径成分 **Π_i = Σ_更新 log(‖W̃_i‖_更新後 / c_i)** を「圧力」として積算し、タスク終端で書き出す。
- **`dclamp`（深さクランプ）**: 更新後に **b ← b + δ·1**（全ユニット同じスカラー δ、double で決める）で、設定点の平均 **s̄ = (1/100) Σ_i (Σ_j W_ij·μ + b_i)**（μ = `mu_mean`）を開始時の s̄(20) に戻す。W は触らない。積算 **K = Σδ**（クランプ補正）と、学習が動かした分 **Δs̄_learned(t) = s̄_更新後クランプ前(t) − s̄(20) − K(t−1)** を記録する。恒等式 s̄(t) − s̄(20) = Δs̄_learned + K を検算。

leaky は正斉次なので `wclamp` の尺度を W2 が機能的に補償し得る。補償は止めない（W2 列ノルムを記録して報告する）。`dclamp` は関数を直接変える。両腕とも test 精度を必ず併記する。

## 2. 測定（毎タスク 更新 20 / 300 / 625、625 で全 test 精度）
[[幅と沈下の介入_spec_0909]] と同じ probe（seed ごとの test512）・同じ参照 perm 8 本（`torch.Generator().manual_seed(20260909)`）:
- 現 perm: z̄_cur・σ_cur・between_cur・**p⁺_cur**（(標本,ユニット) 対で z>0 の割合）・**u⁺_cur**（frac(z̄ᵢ>0)）・ゲート平均 φ′
- 参照 perm 8 本: z̄_inv・σ_inv・between_inv・p⁺_inv・u⁺_inv
- W 統計: 行平均の平均、‖W̃_i‖ の平均、bias 平均、s̄、W2 列ノルム平均
- クランプ帳簿: `wclamp` は Π_i（per-unit・平均）、`dclamp` は K・Δs̄_learned
- ユニット別 z̄ᵢ・σᵢ（inv と cur）・‖W̃_i‖・Π_i をタスク終端で npz に
- SNA は alpha の中央値
**勾配を作る集合（訓練ストリーム）と z̄ を測る probe は別物。σ_cur と σ_inv も別物。p⁺ の主判定は現 perm（ゲートが実際に見る量）で、inv は併記。**

## 3. 判定（事前登録）
seed ごとに、同 seed の `ref` との対で定義する（同じ開始状態・同じストリーム）。**窓は固定**: 「late」= **t61–100**（40 点）、「total」= t20 → t91–100 平均。3 seed とも同じラベルのときだけ付ける。CI・有意差なし。極値の選択なし。

**参照量**（各 seed、`ref`）: late 傾き **b_Z,ref** = z̄_inv を task に OLS 回帰した傾き（t61–100）、**b_S,ref** = σ_inv の同傾き、**p⁺_ref^late** = p⁺_cur の t61–100 平均。見込み（事後値）: b_Z,ref ≈ −0.013/task、b_S,ref ≈ +0.010/task、p⁺_ref^late ≈ 0.16 (LR)・0.29 (SNA)。

**A. `wclamp` — 幅を止めると沈下は止まるか**
- 後期沈下比 **R_A = b_Z,A / b_Z,ref**。3 seed とも R_A < 0.25 → `SINK_STOPS_WITH_WIDTH`；> 0.75 → `SINK_AUTONOMOUS`；他 `SINK_PARTIAL`。
  前期（t21–40）は開始時の力の残り（ラグ）で沈み得る。[[沈下の機構_事後解析_0909]] §2 の静的力は κ≈0.85 で零を横切るので、幅固定でも t20 より 0.2–0.3 深い所まで沈んでから止まる読みがある。ラグを主判定に混ぜないため後期窓を使う。total 比 R_A^total = [z̄_A(91–100) − z̄(20)] / [z̄_ref(91–100) − z̄(20)] は併記。
- 上端 **Δp_A = p⁺_A^late − p⁺_ref^late**。3 seed とも |Δp_A| < 0.03 → `EDGE_HELD`；Δp_A < −0.05 → `EDGE_DETACHES_DOWN`；他 `EDGE_PARTIAL`。（等価限界 0.03、効果限界 0.05。p⁺ の 1 点の seed 内 sd は 0.011–0.016、40 点平均なら 0.003。）
- 副（ラベル無し）: 圧力 Π（t21–100 の平均 per-unit）を ref の log(σ_inv(100)/σ_inv(20)) と並べる（圧力が続くか）。W2 列ノルムの ref 比。精度差（2 pt 超で★）。between_inv の ref 比。

**B. `dclamp` — 沈下を止めても幅は伸びるか**
- 後期幅比 **G_B = b_S,B / b_S,ref**。3 seed とも G_B > 0.75 → `WIDTH_GROWS_REGARDLESS`；< 0.25 → `WIDTH_NEEDS_SINK`；他 `WIDTH_PARTIAL`。‖W̃‖ 版の比を併記。
- 上端 **Δp_B = p⁺_B^late − p⁺_ref^late**。3 seed とも Δp_B > +0.05 → `EDGE_LIFTS`；|Δp_B| < 0.03 → `EDGE_HELD_WITHOUT_SINK`；他 `EDGE_PARTIAL`。
- 副: **K_B = K(100) / −[z̄_ref(91–100) − z̄(20)]**（深さを固定しても学習の押し下げが続くか・増幅するか。ラベル無し）。between_inv の ref 比。精度差（2 pt 超で★）。

**組み合わせの読み**（結果を見る前に固定）
| A | B | 読み |
|---|---|---|
| `SINK_STOPS_WITH_WIDTH` + `EDGE_HELD` | `WIDTH_GROWS_REGARDLESS` + `EDGE_LIFTS` | **幅が先。** 沈下は幅への追随で、張り付きはその帰結。駆動源問題は「幅があるとなぜ下向きか」に還元 |
| `SINK_AUTONOMOUS` + `EDGE_DETACHES_DOWN` | `WIDTH_NEEDS_SINK` | **沈下が先。** 幅は沈下の結果。張り付きは「沈んだ分だけ伸びる」 |
| `SINK_AUTONOMOUS` + `EDGE_DETACHES_DOWN` | `WIDTH_GROWS_REGARDLESS` + `EDGE_LIFTS` | **独立な二過程。** 張り付きは同じ時間則の偶然の同期で、p⁺ 一定は法則ではない |
| `SINK_STOPS_WITH_WIDTH` | `WIDTH_NEEDS_SINK` | 双方向の拘束。どちらが先とは言えない |
| — | `EDGE_HELD_WITHOUT_SINK` | 深さを固定しても占有率が保たれる ＝ 占有率の調節は W̃ の向き（整列）で速く、深さの漂流は別の遅い過程（[[上端の張り付き_事後_0909]] §3 の「1 タスクで揃う」と整合）。この場合 p⁺ は深さの制御変数ではなく、A の判定だけが残る |
| `SINK_PARTIAL` / `WIDTH_PARTIAL` | — | 3 seed の R_A・G_B を連続量として報告し、ラベルは付けない |

**進捗ゲート G1**: `ref` が §1 の (i)(ii)(iii) をすべて満たす。1 つでも外れたら全腕無効で停止。
**G2（クランプの実効性）**: `wclamp` は毎更新後 |‖W̃_i‖ − c_i| / c_i ≤ 1e−6（float32）を全ユニット・全更新で満たす（硬い検査。破れたら停止）。σ_inv が t21–100 で開始値の **±15%** を超えたら判定に★（‖W̃‖ の固定が前活性幅の固定になっていない旨を報告。ref は t40 で +37%）。`dclamp` は毎更新後 |s̄ − s̄(20)| ≤ 1e−6（double、硬い検査）。

## 4. 事前予測（記名）
- **Claude**: A = `SINK_STOPS_WITH_WIDTH` ＋ `EDGE_HELD`；B = `WIDTH_GROWS_REGARDLESS`（G_B ≥ 1。負側ゲートが開くぶん堆積が増えて加速する側）＋ `EDGE_LIFTS`。組み合わせは第 1 行「幅が先」。副で K_B > 1（深さを固定すると押し下げは増幅）。根拠: κ0.7 で深さが 5–10 タスクで上向きに応答したこと（幅より深い状態から戻る力がある）、静的力が κ≈0.85 で零を横切る復元型であること、直交堆積が新タスク由来で深さに依らないこと。**最も弱いのは B の `EDGE_LIFTS`**: 占有率は W̃ の整列でも 1 タスクで調節されるので `EDGE_HELD_WITHOUT_SINK` に落ちる可能性がある。その場合でも A の判定は独立に立つ。
- **Issa**: 追補で登録する（結果を読む前）。仮説「幅が先」がそのまま第 1 行なら両者一致で、走は確証走。分岐するなら B の `EDGE` か A の `PARTIAL` の見込み。
- 通算: Issa 8.5/9、Claude 2.5/9。重みは Issa 側。
- 反証の形: `wclamp` で後期も ref の 3/4 以上の速さで沈めば Claude 外れ（沈下は幅に依らない）。`dclamp` で幅の伸びが 1/4 以下に落ちれば「幅→深さの一方向」が外れる。

## 5. 検算と変異対照
すべての検査に変異対照を付け、対照が実際に検査を落とすことをアサートする（空虚な S 検査を 6 回作った履歴があるため）。
1. **G1 (i)(ii)(iii)** の厳密再現。対照: 開始状態の W1[0,0] に +1e−3 → t21 終端で maxabs > 1e−4。
2. **生成器の継続**: t22..40 で引いた perm が保存 `boundaries[j]['perm']` と exact 一致。対照: perm 生成器を 1 回余分に進めると不一致。
3. **`wclamp` 恒等式**: 毎更新後 |‖W̃_i‖ − c_i|/c_i ≤ 1e−6；クランプ前後で行平均 m_i と b_i は差 0；W2・W3 は差 0。対照: 1 更新だけクランプを飛ばすと ‖W̃_i‖ が c_i から > 1e−5 外れる（float32 で検出可能な最小の更新幅は 1e−3 級）。
4. **`dclamp` 恒等式**: 毎更新後 |s̄ − s̄(20)| ≤ 1e−6；W1 はクランプ前後で差 0；b の変化はスカラー（max − min = 0）；帳簿 s̄(t) − s̄(20) = Δs̄_learned + K が ≤ 1e−9。対照: δ を 0.99δ にすると t21 終端で |s̄ − s̄(20)| > 1e−4。
5. **測定の非侵襲**: `ref` は測定込みで G1 を通す。対照: 測定側で W1 に +1e−9（LR）／act.update（SNA）を入れると G1 (i) が落ちる。
6. **G2 の対照は `ref` 自身**（σ_inv +37%/t40、s̄ の漂流 −0.8/t40）。
失敗は隠さず停止する。

## 6. 集計・保存・運用
seed 内は時系列（80 タスク × 3 phase）、seed 間は 3 本並記。ラベルは 3/3 一致のみ。加法恒等式（帳簿）は seed 単位で検算。
CPU 1 thread・最大 2 process（並列数をコア数に合わせない）。1 継続 **400 秒上限**（`task20to100_0908` の実測は 80 タスクで ≈ 30 秒＋測定。クランプの追加コストは行ノルム 100 本／更新で無視できる）。全体 ≈ 20–40 分。
**3 commit 構成**: spec 単独 commit → 実装 commit → 結果 commit。CSV・verdict・summary・図を commit、ユニット別 npz は SHA 照合付きで `obsidian-research-data/width_sink_clamp_0909` へ。既存 run の出力は上書きしない。
**限界**: 開始状態は t20 の一系列（CPU 再生）。クランプは関数保存でない（`wclamp` は leaky でも W2 の補償を要し、`dclamp` は関数を直接変える）。`wclamp` が固定するのは ‖W̃_i‖（重み空間）であって σ_inv,i そのものではない（G2 で ±15% を要求）。ユニット単位の結合は検定しない（集団の主張）。`wclamp` の毎更新射影は Nero 型の制約付き Adam と同型（[[文献照合_W増大メカニズム_0909]] §6）で、その意味で「WD 以外の幅規制」の陽性対照を兼ねる。leaky では折れ目シフト ≡ bias シフトなので「折れ目を動かす」腕は設けない（[[上端の張り付き_事後_0909]] §4）。

## Log — Claude が裁定した未決（Issa 未確認・結果を読む前に変えてよい）
1. `dclamp` は**集団平均** s̄ を固定する（ユニット別 star_i の固定ではない）。集団の主張に合わせた最小介入。ユニット間の広がりは自由で、between_inv を副測定に置いた。
2. `wclamp` は**ユニット別**ノルム c_i を固定する（全行共通のスカラーではない）。κ 腕と同じ「行ごとの幅」の定義に合わせた。
3. 地平線は **80 タスク（t21–100）**。20 タスクでは後期窓が取れない。参照は既存の `task20to100_0908` と `task100.pt` で bit 一致が取れる。
4. 腕は LR/none・SNA/none のみ。l2 は沈まない、ELU は task20 の保存状態が無い。
5. p⁺ の主判定は**現 perm**。inv は併記。
6. 後期窓は **t61–100 の OLS 傾き**（単点差は 1 タスクのジッタ ±0.1 で 0.25 閾値が割れない）。
7. 占有率クランプ（p⁺ を b で固定する腕）は**設けない**: 自然軌道が既に p⁺ 一定なので、クランプは偏差にしか働かず `ref` と区別できない。

---

## 追補 1（2026-09-09・**実装前・結果ゼロの時点**）: 3 クランプ化・ELU 追加・Issa の予測

**登録時点の状態**: 実装は 1 行も書いていない。走はゼロ。本追補は §1–§6 を置き換える（差分を以下に明示し、置き換えられた側は取り消さず残す）。

### A1. 追補の動機となった事後観測（`edge_pin_posthoc_0909` の追加集計・結果ではなく設計材料）

1. **沈下とユニット間の幅は、同じ 1 本のベクトル（W1 の行平均 m_i）の平均と散らばりである。** `task20to100_0908` の `star` 分解（素 leaky seed0）: sd(star) 1.374 → 2.797 のうち **行平均由来 sd(star_W) が 1.383 → 2.743**、bias 由来 sd(bias) は 0.128 → 0.195 でほぼ寄与しない。平均側も mean(star_W) −1.809 → −3.723 に対し mean(bias) +0.042 → −0.128 で、**沈下の 92% は m̄ が運ぶ**（[[沈下の機構_事後解析_0909]] の「変位の 99.2% が W 経由」と整合）。
2. **行平均は、ユニット内の幅の 41% も作っている。** `width_depth_intervention_0909` の分解（素 leaky seed0・t20）: σ²_inv = 1.560 = off 0.647 + wid 0.916 + cross(≈ −0.004)。off_i = m_i²·s11、s11 = 1ᵀΣ1 ≈ 1307（MNIST の明度方向の分散）。**つまり m̄ が沈むこと自体が off = s11·E[m²] を増やし、ユニット内の幅を広げる。** 沈下 → 幅の逆向き経路が構成として存在する。
3. **2 つの幅の成長率は活性化で分かれる**（t20→100、3 seed、`elu_growth_0909` の `between_inv`/`sigma_inv`）: leaky はユニット間 ×2.02/2.35/1.59・ユニット内 ×2.04/2.05/2.04 で**同率**、ELU α=1 はユニット間 **×3.67/3.93/4.15** 対ユニット内 ×2.95/2.81/3.06 で**分離する**（B/W 比 1.8 → 2.4）。

→ 初版の `wclamp` は ‖W̃ᵢ‖ しか固定せず、行和は W̃ の定義から不変なので **ユニット間の幅も off も素通り**する。leaky では 2 軸が同率で伸びるため、初版のままでは `SINK_PARTIAL` が出たときに「幅は駆動源でない」と「もう片方の幅が代わりに運んだ」を分けられない。**初版 §1 の 2 腕構成は、この理由で不足と判断して差し替える。**

### A2. 腕の差し替え（§1 を置換）

W1 の行を **W_i = m̄·1 + (m_i − m̄)·1 + W̃_i** と 3 成分に分ける（R^784 上で互いに直交する部分空間への分解。第 1・2 成分は全 1 方向を「ユニット横断の水準」と「ユニット間のばらつき」に割ったもの、第 3 成分はその直交補空間）。**クランプはこの 3 成分に 1 本ずつ当てる。** 毎更新、Adam 更新の直後に適用する。Adam の m・v・t、Snake の V、bias、W2、W3 は**どの腕でも触らない**。

| 腕 | 固定するもの | 実装（更新後・double で計算し float32 へ戻す） | 厳密に不変なもの |
|---|---|---|---|
| `ref` | — | 無介入 | — |
| `wclamp` | 各行の中心化ノルム ‖W̃ᵢ‖ = c_i（開始時の値） | W̃ᵢ ← W̃ᵢ·c_i/‖W̃ᵢ‖ | m_i 全部（∴ star_i・s̄・ユニット間の幅・off） |
| `mclamp` | 行平均の散らばり sd_i(m_i) = σ_m(開始時) | m_i ← m̄ + λ(m_i − m̄)、λ = σ_m/sd_i(m_i)（行 i に定数 (λ−1)(m_i−m̄) を足す） | W̃ᵢ 全部・m̄（∴ s̄・‖W̃ᵢ‖・wid） |
| `dclamp` | 行平均の水準 m̄ = m̄(開始時) | m_i ← m_i + (m̄₀ − m̄)（全行に同じ定数） | W̃ᵢ 全部・sd_i(m_i)（∴ ‖W̃ᵢ‖・wid・ユニット間の幅） |

**初版から変えた点と理由**:
- `mclamp` を新設（A1-3 が理由）。
- **`dclamp` を b 経由から m̄ 経由に変更した。** 初版は「b にスカラーを足して観測量 s̄ を固定」だったが、それだと m̄ は自由に沈み続け、A1-2 の経路で off = s11·E[m²] が伸びるので、**沈下を止めたつもりで幅を伸ばす原因を残す**。沈下の 92% は m̄ が運ぶので、m̄ を固定するのが「沈下を止める」の実体。観測量 s̄ = m̄·S̄ + b̄ は b̄ の漂流ぶんだけ動く（素 leaky の実測で 80 タスク 0.17、自然な沈下 2.1 の 8%）ので、**b̄ 由来の残留沈下を G3 で検証**する。
- 3 腕は**パラメータ空間では厳密に直交**するが、**観測量では直交しない**（`mclamp` は off = s11(m̄²+var(m)) を通じて σ_within を下げる。素 leaky t100 の見積りで −5.5%、同期間の自然成長 +104% に対し十分小さい）。**「観測量が直交する」とは主張しない。** 各腕が σ_inv・between_inv・p⁺ に何をしたかは全部測って報告する。
- 初版 §3 の `wclamp` 用 G2「σ_inv が ±15%」は **`mclamp` にも適用**し、閾値を **±20%**（off 経由の副作用を許す幅）に緩める。破れたら★（当該腕が二重の仕事をしている旨を報告）。

### A3. 開始点と腕の集合（§1 を置換）

保存 t20 状態からの継続を**やめ、init から回す**。`H.init_params(seed,cpu)` と `H.stream('perm'|'data'|'batch',seed)` で t1 から学習し、**t1–20 は無介入、t21–100 でクランプ**（開始値 c_i・σ_m・m̄₀ は **t20 終端**の値）。理由: ELU には task20 の保存状態が無いが、`elu_growth_0909` が init 経路で committed checkpoint 42 点を maxabs 0.0 で再現済みなので、**init から回しても `ref` の bit 一致は取れる**。972MB の source 依存も外れる。

腕: **LR/none・SNA/none・ELU1/none × {ref, wclamp, mclamp, dclamp} × seed 0..2 = 36 継続**（＋seed0 の変異対照）。ELU の実装は `src/elu_growth_0909.py` の `ELU` クラスを import して使う（複製しない）。l2 腕は沈まない（p⁺ 0.5）ので入れない。

**G1（差し替え）**: `ref` 腕は次を満たすこと。1 つでも外れたら全腕を無効として停止する。
- LR/SNA: `results/boundary_groups_0908/{arm}_none_s{seed}_task1.pt` と `_task100.pt` の `state`・`adam`・`perm`・`streams` が **exact 一致**。
- LR/SNA: `results/task20to100_0908/{arm}_none_s{seed}.npz` のタスク終端 81 点（`zmean`・`star`・`within`）を **maxabs ≤ 1e−10** で再現。
- 3 活性化とも: `results/elu_growth_0909/{arm}_s{seed}_units.npz` の `cnorm_i`・`zbar_i`・`sd_i` のうち **t1–100 の 100 点**を **maxabs ≤ 1e−10** で再現（ELU1 の唯一の bit 級参照）。
- 変異対照: 初期値 W1[0,0] に +1e−3 を足すと上のいずれも > 1e−4 で落ちること。

### A4. 判定の差し替え（§3 を置換）

参照量・窓は初版どおり（late = **t61–100**、40 点、OLS 傾き、3 seed 一致でのみラベル）。腕ごとに `ref` との対で:

**A1 `wclamp` / A2 `mclamp` — 幅の片側を止めると沈下は止まるか**
各クランプ X について **R_X = b_Z,X / b_Z,ref**（b_Z = z̄_inv の late 傾き）。3 seed とも R_X < 0.25 → `SINK_STOPS_{W,M}`；> 0.75 → `SINK_SURVIVES_{W,M}`；他 `SINK_PARTIAL_{W,M}`。
上端 **Δp_X = p⁺_X^late − p⁺_ref^late**: 3 seed とも |Δp| < 0.03 → `EDGE_HELD`；< −0.05 → `EDGE_DETACHES_DOWN`；> +0.05 → `EDGE_LIFTS`；他 `EDGE_PARTIAL`。
副（ラベル無し）: もう一方の幅の応答（`wclamp` での between_inv 比、`mclamp` での σ_inv 比）、圧力 Π（除去した成分の対数和）、W2 列ノルム比、精度差。

**B `dclamp` — 沈下を止めても幅は伸びるか**
**G_B^within = b_S,B / b_S,ref**（σ_inv の late 傾き）と **G_B^between = b_B,B / b_B,ref**（√between_inv の late 傾き）。両方が 3 seed とも > 0.75 → `WIDTH_GROWS_REGARDLESS`；両方 < 0.25 → `WIDTH_NEEDS_SINK`；片側だけ > 0.75 → `WIDTH_GROWS_{WITHIN,BETWEEN}_ONLY`；他 `WIDTH_PARTIAL`。
上端 Δp_B は上と同じ 4 値。
副: K = 除去した m̄ の累積、b̄ の漂流、精度差。

**読み**（結果前に固定）
| 結果 | 読み |
|---|---|
| `SINK_STOPS_W` か `SINK_STOPS_M` の少なくとも一方 ＋ `WIDTH_GROWS_REGARDLESS` | **幅が先。** どちらのラベルが立ったかで「どの幅が駆動源か」まで決まる |
| 両方 `SINK_STOPS_*` | 2 軸のどちらを止めても沈下が止まる ＝ 沈下は「幅一般」に従う |
| 両方 `SINK_SURVIVES_*` ＋ `WIDTH_NEEDS_SINK` | **沈下が先。** 幅は沈下の結果 |
| 両方 `SINK_SURVIVES_*` ＋ `WIDTH_GROWS_REGARDLESS` | **独立な二過程。** p⁺ 一定は同じ時間則の偶然の同期で、法則ではない |
| `SINK_STOPS_*` ＋ `WIDTH_NEEDS_SINK` | 双方向の拘束 |
| B が `EDGE_HELD` | 深さを止めても上端が保たれる ＝ 占有率の調節は速い成分（W̃ の向き）が担い、深さの漂流は別の遅い過程 |

**G3（進捗ゲート・新設。初版の★を置換）**: クランプ腕は「学習が壊れていない」ことを示す。**(a)** t61–100 のタスク終端 test 精度の中央値が `ref` の中央値 − **5 pt** 以上、**(b)** タスク内の CE 改善（probe 上、更新 0 → 625）が t61–100 の **90% 以上のタスクで正**。どちらか外れた腕seed は主判定から外し **`NOT_TESTABLE_LEARNING_BROKEN`**（連続量は報告する）。精度が `ref` より**高い**のは失敗ではない（クランプが正則化として効いた場合）。そのまま報告する。
**G4（`dclamp` の実効性）**: b̄ 由来の残留沈下 |z̄_B(91–100) − z̄_B(20)| が `ref` の同区間の沈下の **0.25 倍未満**。超えたら B 腕は `NOT_TESTABLE_DEPTH_NOT_HELD`。

### A5. 事前予測（記名・**結果ゼロの時点**）

- **Issa**: 「**幅が先**」。B の上端は **`EDGE_HELD`（深さを固定しても p⁺ は保たれる）または判定不能**。どちらの幅が先かは指定しない（本走が決める）。
- **Claude**: A は `SINK_STOPS_W` かつ `SINK_STOPS_M`（どちらの軸を止めても止まる）＋ 両方 `EDGE_HELD`；B は `WIDTH_GROWS_REGARDLESS`＋**`EDGE_LIFTS`**。
- **唯一の分岐点は B の上端**: Issa `EDGE_HELD` 対 Claude `EDGE_LIFTS`。`EDGE_PARTIAL` なら両者外れ。A 側は両者一致（「幅が先」）なので確証走であり、**A で `SINK_SURVIVES_*` が両方立てば両者そろって外れる**。
- 通算: Issa 8.5/9、Claude 2.5/9。重みは Issa 側。
- Claude の予測の弱いところ: `SINK_STOPS_M` は、m̄ を自由にしたまま sd(m) だけ固定して沈下が止まる理由が言えていない（A1-2 の off 経路なら `mclamp` はむしろ弱いはず）。ここが外れる可能性は `SINK_STOPS_W` より高い。

### A6. 検算の差し替え（§5 に追加。初版 1・2・5・6 は生きている。初版 3・4 は下で置換）

すべての検査に変異対照を付け、対照が実際に検査を落とすことをアサートする。
- **C3 `wclamp`**: 毎更新後 max_i |‖W̃ᵢ‖ − c_i|/c_i ≤ 1e−6；m_i は差 0（∴ 行和・star も差 0）；b・W2・W3 は差 0。対照: 1 更新だけクランプを飛ばすと > 1e−5。
- **C4 `mclamp`**: 毎更新後 |sd_i(m_i) − σ_m|/σ_m ≤ 1e−6；|m̄ − m̄_更新後クランプ前| ≤ 1e−12（共通平均は動かさない）；‖W̃ᵢ‖ は全 i で相対 ≤ 1e−6 で不変；b・W2・W3 は差 0。対照: λ を 1/λ にすると sd 検査が > 1e−2 で落ちる。
- **C5 `dclamp`**: 毎更新後 |m̄ − m̄₀| ≤ 1e−9；sd_i(m_i) は相対 ≤ 1e−6 で不変；‖W̃ᵢ‖ 不変；b・W2・W3 は差 0。対照: 補正を 0.99 倍すると t21 終端で |m̄ − m̄₀| > 1e−4。
- **C6 分解の恒等式**: 全腕・全測定点で σ²_inv = off + cross + wid（相対 1e−5、`width_depth_intervention_0909.decompose` と同じ実装）かつ z̄_inv,i = star_i（絶対 1e−6）。対照: off の m_i を 1.01 倍すると落ちる。
- **C7 直交性**: `mclamp`・`dclamp` の介入前後で ‖W̃ᵢ‖ が相対 1e−6 で不変、`wclamp` の介入前後で m_i が差 0。これが 3 成分がパラメータ空間で直交していることの数値的な確認。対照: 各クランプを「行全体のスケーリング」に変えると落ちる。

### A7. 運用（§6 に追加）

1 継続 ≈ 100 タスク。`elu_growth_0909` の実測は 120 タスクで 44 秒＋測定なので、**1 継続 400 秒上限**は据え置き。36 継続・最大 2 process で **合計 40–60 分**の見込み。
**実装は外部エージェント（Codex）に委譲する**（[[運用ルール]] §2「大規模な実装は委譲してよい。事前登録は委譲元が先に固定し、実行結果は独立に検証してから取り込む」）。本追補が委譲前の凍結点であり、**Codex には結果を一切見せない**。取り込み前に Claude が敵対的レビュー（spec 適合／対称性・bit 一致／変異）を通す。

---

## 追補 2（2026-09-09・**実装中・走ゼロ・本走の結果は存在しない**）: 参照腕の傾きで比の可検定性を先に固定する

**登録時点の状態**: `src/width_sink_clamp_0909.py` は Codex が起草中で、まだ 1 度も走っていない。本追補が使う数値は**すべて既発表の `elu_growth_0909`（結果 commit `07998e4`）の `ref` に相当する自然軌道**から取ったもので、本走の結果ではない。閾値は「比較したい両側が別ラベルになることを確認してから凍結する」（[[運用ルール]] §5 教訓⑫）に従った設計作業である。判定規則の**追加**であって、腕・クランプ・測定・既存の閾値は変えない。

### B1. 参照腕の late 窓の傾き（既発表・タスク終端・OLS・per task）

| 腕 | b_Z（z̄_inv） | b_S（σ_inv） | b_B（√between_inv） |
|---|---|---|---|
| LR | −0.02115 / −0.02335 / −0.01530 | +0.01341 / +0.01308 / +0.01145 | +0.01999 / +0.01673 / +0.00883 |
| SNA | **−0.00372 / −0.01116 / −0.00103** | +0.00651 / +0.00880 / +0.00812 | **+0.00442** / +0.02253 / +0.01812 |
| ELU1 | −0.03206 / −0.03844 / −0.03340 | +0.02019 / +0.02217 / +0.02344 | +0.04394 / +0.06057 / +0.06291 |

**SNA は late 窓でほとんど沈んでいない**（seed2 は −0.001/task ＝ 40 タスクで 0.04）。R_X = b_Z,X/b_Z,ref はゼロ割りに近く、クランプが何をしても比が桁で振れる。t21–60 窓でも SNA の b_Z は −0.005〜−0.010 で LR の 1/4〜1/7 なので、窓を変えても解決しない。**窓は t61–100 のまま据え置き、代わりに可検定性のゲートを置く。**

### B2. G5（可検定性ゲート・新設）

**比のラベルは、その比の分母が参照腕で十分大きい seed でだけ付ける。**
- R_X（A1・A2 の沈下比）: `|b_Z,ref| ≥ 0.005` /task（40 タスクで 0.2 以上沈む）
- G_B_within: `b_S,ref ≥ 0.005` /task ／ G_B_between: `b_B,ref ≥ 0.005` /task

満たさない arm×seed はその比について **`NOT_TESTABLE_REF_FLAT`**。ラベルは**可検定な 3 seed が 3/3 一致したときだけ**付け、可検定 seed が 3 未満なら腕ごと `NOT_TESTABLE_REF_FLAT`（連続量は全部報告する）。
既発表の参照値をこの規則に当てると、**LR と ELU1 は 3 指標とも 3/3 可検定、SNA は R_X が 1/3・G_B_between が 2/3 で不可検定、G_B_within のみ 3/3 可検定**になる見込み。**つまり A1・A2 の主判定は実質 LR と ELU1 で決まる。** これを結果が出る前に書いておく。
Δp（上端）は比ではないので本ゲートの対象外。全腕で判定する。

### B3. 追補 1 の許容値 2 点の事前修正

- **C5 の `|m̄ − m̄₀| ≤ 1e-9` は float32 の丸めに対して際どい。** 独立に書いたオラクル実装（`m` と `W̃` を double で作り float32 へ書き戻す）で実測 4.9e−10 だった。桁は足りているが余裕が 2 倍しかないので、**許容を 1e-8 に緩める**（実装前に決める。走らせてから緩めない）。変異対照（補正 0.99 倍で > 1e-4）は判定力を保つ。
- **`wclamp` が固定するのは ‖W̃ᵢ‖（重み空間のノルム）であって σ_within ではない。** W̃ᵢ の**向き**は自由なので、入力共分散の大きい部分空間へ回れば ‖W̃ᵢ‖ 一定のまま `wid` = W̃ᵢᵀΣW̃ᵢ は増えうる。したがって `SINK_STOPS_W` は「中心化重みのノルムが駆動源」であって「ユニット内の幅が駆動源」ではない。**σ_inv と var_wid を必ず併記し、追補 1 A2 の ±20% フラグで回転の有無を読む。**

### B4. 予測への影響

追補 1 A5 の記名予測は変えない。可検定性ゲートにより、**Issa と Claude の分岐点（B 腕の上端 `EDGE_HELD` 対 `EDGE_LIFTS`）は 3 腕すべてで判定できる**（Δp は比ではないため）。A 側の一致部分は LR と ELU1 で判定される。

---

## 追補 3（2026-09-09・**実装中・走ゼロ**）: 追補 1 の C6b は偽の恒等式だった。訂正と、ユニット間の幅の定義

**登録時点の状態**: 本走はまだ 1 度も走っていない（実装は起草中）。本追補は追補 1 A6 の検査 1 本を**訂正**し、判定量 b_B の定義を**厳密な量に差し替える**。腕・クランプ・窓・閾値・記名予測は変えない。

### C1. 何が間違っていたか

追補 1 A6 の C6 後半「`max_i |z̄_inv,i − star_i| ≤ 1e-6`」は**成り立たない**。保存 t20 状態（素 leaky seed0）で実測すると **0.681**。
理由: `star_i = m_i·784μ + b_i` は**訓練集合の平均画素 μ から作った理論値**だが、`z̄_inv,i` は**プローブ 512 標本を参照 perm 8 本で測った標本平均**である。行を W_i = m_i·1 + W̃_i と分けると、任意の perm r について
$$\bar z_{r,i} \;=\; m_i\,\bar S \;+\; b_i \;+\; \widetilde W_i\cdot \bar x_r ,\qquad \bar S=\overline{\mathbf 1^{\mathsf T}x}$$
が**厳密に**成り立つ（1ᵀx は置換不変）。最後の項は perm ごとにゼロ平均だが、8 本平均では消えない残差 ε_i になる。実測で `max_i |z̄_inv,i − (m_i S̄ + b_i)| = 0.698`、ユニット間 sd は z̄_inv 1.428 対 star 1.374、相関 0.988。**ε は無視できない。** また μ（訓練平均 0.13066、784μ = 102.44）とプローブの平均総輝度 S̄ = 103.74 も別物である。

これは実装前に見つけた**私（Claude）の設計ミス**で、そのまま走らせれば最初の測定で停止していた。

### C2. C6b の差し替え（厳密な恒等式に）

**C6b'**: 各参照 perm r と各ユニット i について
$$\max_{r,i}\;\bigl|\,\bar z_{r,i} - (m_i\bar S + b_i + \widetilde W_i\cdot\bar x_r)\,\bigr| \le 10^{-9}\quad(\text{float64})$$
を全測定点で検査する。これは行分解が測定した z と整合していることの厳密な検査であり、`m` を 1.01 倍する変異対照で落ちる。
併せて **ε の大きさ `eps_max` = max_i |z̄_inv,i − (m_i S̄ + b_i)| を毎測定で記録**する（ラベル無し。8 本平均の残差が腕やタスクでどう動くかを見るため）。

### C3. ユニット間の幅は `star_sd` で測る（b_B の定義変更）

追補 1 A4 の `b_B` は「√between_inv の late 傾き」だったが、`between_inv` は上の ε を含む**8 本標本の量**である。**厳密な量に差し替える**:
$$\texttt{star\_sd}(t) \;=\; \operatorname{sd}_i\bigl(m_i\cdot784\mu + b_i\bigr)$$
これは perm に依らず params だけから決まり、`mclamp` が固定する `sd_i(m_i)` と直接対応する（b_i の寄与は素 leaky で sd 0.13 対 1.37 と小さい。追補 1 A1-1）。**`b_B` = `star_sd` の late 窓 OLS 傾き**とし、`√between_inv` の傾きは**併記のみ**にする。

既発表の参照軌道（`task20to100_0908` の per-unit `star`、t61–100）で当てると:

| 腕 | b_B(star_sd) | 参考: b_B(√between_inv) | star_sd の水準 t20→t100 |
|---|---|---|---|
| LR | +0.01856 / +0.01604 / +0.00870 | +0.01999 / +0.01673 / +0.00883 | 1.37→2.80 / 1.05→2.48 / 1.21→1.95 |
| SNA | **+0.00161** / +0.02020 / +0.01811 | +0.00442 / +0.02253 / +0.01812 | 0.73→1.10 / 0.82→3.02 / 0.88→2.94 |

2 つの定義は数値的にほぼ同じ順序で、**追補 2 の G5 ゲート（|b_ref| ≥ 0.005/task）の適用結果も変わらない**（LR 3/3 可検定、SNA は seed0 が不可検定）。ELU1 は committed 出力に per-unit `star` が無いので事前の値が出せない。**ELU1 の可検定性は本走の `ref` 腕の b_B に G5 を当てて決める**（規則は先に凍結されており、値を見てから閾値を動かすことはしない）。

### C4. 記録項目の追加（追補 1 A2 の測定に足す）

毎測定で `star_sd`（上の定義）と `eps_max`（C2）を記録する。既存の列は削らない。判定は本追補の `b_B` を使い、`√between_inv` 版は summary に併記する。
