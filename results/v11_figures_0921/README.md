# V11 原稿の図 — 図 1–9（0921）

作図スクリプトは `analysis/v11_figures_0921/`。committed CSV だけを読み、走は起こさない。

```
python3 analysis/v11_figures_0921/make_all.py     # 全部作り直す
python3 analysis/v11_figures_0921/fig02_doors.py  # 1 枚だけ
```

体裁は vault `可塑性喪失/主張/V11原稿_図表一覧と9月30日までの計画_0921` §2.5 の作図規約に従う。
規約の実装は `style.py` に集約してあり、色・ラベル・格の表示・登録窓の帯・精度軸の固定は
すべてそこを直せば全図に効く。ラベルは日本語で、英語化は `style.py` の LABEL / AXIS /
GRADE を差し替える（§2.5-9）。

各図の `*.txt` はその図のキャプションの下書き（集約・帯の意味・窓・格・元データ）。

## 体裁の決定（0922・Issa）

版の選択は 4 件とも決まった。出力は 1 図 1 ファイルで、選ばれなかった版は消してある。
スクリプト先頭の `LAYOUT` / `VARIANT` / `HARD` が決定を持つ。

| 図 | 決定 |
|---|---|
| 2 | パネルは **2×2**（1 枚が大きく凡例と格が読める） |
| 3 | **中央値＋全範囲の帯**（他の図と揃える）。(b) の目盛は等間隔 |
| 4 | easy 課題を **点線で添える** |
| 9 | 16 腕は **横棒＋範囲の線**。(b)(c) の Snake・KK 族は **KKT1 だけ**（E1 で KKT1 = SNA が登録） |

本文の図 2 は主張の梯子 `ref → C → CH → CHB` の 4 腕だけ（0921・Issa）。開ける扉を変えた対照
（`H` 中間層だけ・`CS` 分散だけ・`LN` 素の LayerNorm）は `fig02e_doors_controls` として付録 E に回す。
D2 = CH − C が `H_HELPS`・N = `NEED_CH` なので、H 単独は床のままであることが対照の中身。

## 0922 の訂正（Issa 指摘）

本文から正規化量を外した。図 2 の (b) 沈下比 `z̄₂/sd₂` と (d) `|b₂|/sd₂`、図 3 の (b) `|b₂|/sd₂` は、
分母がゼロに落ちるだけで発散する量だった。`ref` は t50 で `zsd_l2` = 0・`zbar_l2` = −0.037 なので、
比の −3.7e10 と 1.5e11 は分母の写しでしかなく、生の量は動いていない（[[引用禁止]] C
「正規化量で力学を書かない。生の x で測る」）。

- 図 2 のパネル: (a) online・(b) 生の `zbar_l2`・(c) 生の `zsd_l2`・(d) `dead_frac_l2`
- 図 3 の (b): 生の `bias_absmean_l2`

読みも変わる。`ref` は沈んで死ぬのではなく目盛が潰れて死ぬ（`sd₂` → 0）。沈んで死ぬのは `C`
（`z̄₂` −168・`sd₂` 78）。`H` は `ref` 型（`sd₂` 0.079）。登録列 R2（沈下比の t50 中央値 −0.191）は
キャプションの数値として引く。

## 0922 の読みやすさの直し

値が両端に張り付いて位置から読めなくなっていた 5 枚を直した。軸の規約（§2.5-3 の
online accuracy は 0–1.02 固定）は動かさず、**数値を点のそばに添える**か、**軸線に余白を入れる**
ことで解いた。

| 図 | 何が起きていたか | どう直したか |
|---|---|---|
| 2 | `dead_frac` の 0 と 1 が軸線に重なり、`sd₂` = 0 の `ref` が底の枠線に隠れていた | 両端に余白（`style.breathe`）。`ref` は太めに敷いて上の腕の隙間から見えるように |
| 3 | CH と CHB が同じ青の族で、10 本ずつ重なると見分けられなかった | CHB を破線に（`seeds_lines` が線種を取るように） |
| 4 | (c) で 4 腕が 0 に重なり、上の 1 本しか見えなかった | 余白＋「H・CH・KKT1・L2 Init は 0 のまま」と注記 |
| 5 | (b) の境界の自己 S が軸の底に張り付き、格のラベルが点に被っていた | `ylim` を明示、格を移動 |
| 7 | (b) で 4 腕が 0.10–0.12、8 腕が 0.85–0.99 に固まり、上の 8 腕の順位が読めなかった | 数値を添える（`style.value_labels`） |
| 8 | (a) で 6 腕が 0.92–0.97 に固まっていた | 同上 |
| 9 | (a) で 13 腕が 0.58–0.67 に、fresh gap は 13 腕が 0 の近くに固まっていた。(c) は 7 腕ぶんの帯が濁っていた | (a) は数値を添える。(c) は中央値の線だけにし、seed の散らばりは (a) が担う |

`style.py` に 2 つ足した。`value_labels(ax, y, values)` は水平の順位図で点のそばに数値を置き、
`breathe(ax)` は端の値が軸線に張り付かないよう両端に余白を入れる。格のラベルは
`center right` / `center left` にも置けるようにした。

## 付録の図（0922）

付録 A–D・F・J を同じ規約で作った。付録 E は `fig02e_doors_controls`（0922 に図 2 から分けた対照）。
表 2〜4 の数値は `tables_v11.py` が committed CSV から再計算して `tables_2-4_from_csv.md` に書く
（vault の転記との照合用。表の正本は vault `論文作成/V11原稿_表1-5_0922`）。

| 付録 | ファイル | 中身 | 格 |
|---|---|---|---|
| A | `figA_mnist_chain` | MNIST の移植の鎖 5 本（resp_ee・respdyn・swap・escneff・neffdir）の要約と登録ラベル | 登録（seed 平均） |
| B | `figB_5p1_budget` | 5+1 の予算 ×10。CH − KKT1 は `LEVEL`、CH − R は符号反転、train 末は追いつく | 登録（seed 中央値） |
| C | `figC_initgeom_rest` | 初期配置の γ 梯子（`L1_MODEL_MISS`）・第 2 層（`L2_CONDITIONAL_MODEL_MISS`）・bias 0 の副診断 | 登録＋副診断 |
| D | `figD_drive_full` | 駆動の全体。M1x `NOT_SUPPORTED`・M3x `BOUNDARY_ENRICHED`・課題別の S と U | 登録＋記述 |
| F | `figF_kunekune_tail` | kunekune の帯の外に出た質量と K1・K3 | 登録列の読み＋登録 |
| J | `figJ_layer_chimera` | 層別キメラ S-A の 7 セル。online・p⁺・Q・‖µ₂‖ | 登録（Q1 `MIXED`）＋登録列 |

決定 10b（0922）: 図 8 は S4 だけで、MNIST の鎖は付録 A に置き、本文には図を足さない（`BOX_SPECIFIC`）。
決定 11（保留）: 付録 J は本文に出すか未決のまま先に作ってある。

表 2 の照合で 1 か所直した: 草案 §1 の H の t1 online は 0.136 と書かれていたが CSV では 0.626（memo 0.774）。
ref の値の写し間違いで、vault 側を 0922 に訂正。
