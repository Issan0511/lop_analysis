# eg_lag summary — aniso_perp_0812

git_hash: `753f5846d1bd93d8284ec4dfa089a3909766dd3c`  /  non-finite (diverged) run-ckpt rows: 0/360

6 点の checkpoint グリッド ([0, 1e4, 5e4, 1e5, 3e5, 1e6]) しか無いため、**時系列相互相関によるラグの定量化はしない**。ここで判定するのは 「各 ckpt で align(E[g]) >= align(W) が保たれ、ckpt=0 で最大の乖離があるか」という順序判定 (P1/P2) のみ。ラグを定量化するには密グリッドでの再走が必要。

## 事前登録判定

- P1（主判定）: ckpt=0 で mean|cos|(E[g]) > 0.35 かつ mean|cos|(W) < 0.25
- P2: 全 ckpt で mean|cos|(E[g]) − mean|cos|(W) の bootstrap 95%CI 下端 >= 0

## 判定表 (group = followup gname)

| gname | d | chance | Eg@0 (95%CI) | W@0 (95%CI) | P1 | P2 | P2 不成立 ckpt |
|---|---|---|---|---|---|---|---|
| B_w100 | 21 | 0.174 | 0.810 (0.770, 0.847) | 0.176 (0.175, 0.176) | PASS | PASS | — |
| B_w5 | 21 | 0.174 | 0.806 (0.772, 0.840) | 0.186 (0.171, 0.202) | PASS | PASS | — |

## 反証条件チェック (w5 と w100 の両方で P1 と P2 が不成立か)

| exp | batch | w5 group | w100 group | 両方不成立 (=棄却) |
|---|---|---|---|---|
| B | 1 | B_w5 | B_w100 | no |

## cos_self = cos(E[g_i], W_i) の記述統計 (最終 ckpt, alive のみ)

| gname | ckpt | median | IQR |
|---|---|---|---|
| B_w100 | 1e+06 | 0.185 | [-0.180, 0.325] |
| B_w5 | 1e+06 | 0.287 | [0.134, 0.386] |

## 但し書き

- ckpt グリッドが6点のみのため、上記は全て順序判定 (どちらが先に高いか) であり、リード・ラグの時間長 (何 step 先行するか) は推定していない。定量化するには少なくとも ckpt=0〜1e4 の間を対数刻みで細分した密グリッドでの再走が必要 (既存の6点は 0, 1e4, 5e4, 1e5, 3e5, 1e6)。コスト見積り: チェックポイント保存自体の追加コストは小さく、支配項は既存のフル学習と同じ (`RESULTS.md`/メモ記録では w5 は CPU で全体 ~1時間程度、w100 の full batch 系列のみ GPU が必要)。つまり密グリッド化のための再学習コストは既存のフル実行1回分と同程度と見積もる(要ユーザー確認、本解析では新規学習は行っていない)。
- bootstrap は run (seed) 単位のリサンプル 10,000 回、95% パーセンタイルCI。
- aniso_perp_0812 の各 group (`B_w5`, `B_w100`) は kappa in {1,4,16} と lr in {0.003, 0.001} の複数条件を1つの group (npz の R 次元) にまとめて含む。判定は followup の group 単位 (gname) で行っており、kappa/lr ごとの分離判定はしていない (`eg_lag_summary.csv` の kappa/lr 列で個別に再集計可能)。

## 報告文 (8/20 返信用)

「E[g] 整列は ckpt=0 から 0.81（95%CI [0.78, 0.83]）であり、W は 0.18（95%CI [0.17, 0.19]、ランダム 0.17）であり、W の整列に先行する。ラグの定量化には ckpt 密グリッドの再走が必要（コスト見積り：既存のフル実行1回分と同程度、要ユーザー確認）」（aniso_perp_0812 全 group プール、n=60 finite runs）

