# snake_lr_diag_0903 — Snake を lr=0.005・alpha=1 で完走させる診断

> **この走は事前登録されていない。Issa の指示で回した診断で、変えた軸は lr（0.01 → 0.005） の 1 本だけ。**verdict には入れない。** lr が登録値と違うので他腕と同じハーネスではなく、**水準の比較には使わない**。**

- 登録走の `SN_a1_1216`（lr 0.01・alpha 1）は `NUMERIC_DIVERGENCE`（step 1,000・seed 3）
- 本診断（lr 0.005・alpha 1）: S-cap `OK` / 発症 1M **0/10**・5M **0/10** / median log10 U (5M) **-11.8194**
- $k^\ast$ 中央値 500・横断 0/10・横断窓の割合 0.000
- 全ユニット span 中央値 2.053 / |v| 中央値 0.1002 / `frozen_abs` 0

**他腕との水準比較には使わない**（lr が違うので同じハーネスではない）。
