# center_oracle_0831_followup2 summary

> **事後解析の登録。事前登録ではない。** vault `オラクル中心化_spec_0831_追補2` §2 の数値は本走の結果を見た後にチャットで算出済みである（spec §0）。全行 `registered = 0`、`analysis_grade = registered_posthoc_not_preregistered`。

入力は committed の per-seed ログ 20 本と親 `verdict.csv` のみ。新しい走・再計装はしていない。マスクは追補1 の 499 点をそのまま使う。

- **P1'-a（主）**: **LAG_IS_PROTECTIVE**
- **P1'-b**: **LAG_IS_PROTECTIVE**（P1'-a と同符号なので `WINDOW_DEPENDENT` は出ない）
- **P1'-c**: **ABSORPTION_EXACT_UNDER_ORACLE**

## P1'-a / P1'-b 生存中 1 境界あたりの Δβ

ユニットごとに、**直前に alive だった真の切替遷移のみ**を平均する。除外は当該窓の生存境界が 10 本未満のユニット。$K$ = 36（Aexact の生存境界数: per-seed 中央値の中央値 36.2 を四捨五入）。

| 集計範囲 | `L1w100_Aexact` | `L1w100_A1` | $R'$ | ラベル |
|---|---:|---:|---:|---|
| 全 499 境界 | **-0.0509** [-0.0609, -0.0464] | -0.0211 [-0.0263, -0.0195] | **2.336** [2.124, 2.548] | LAG_IS_PROTECTIVE |
| 最初の 150 境界 | **-0.0509** [-0.0609, -0.0464] | -0.0234 [-0.0305, -0.0199] | **2.224** [1.858, 2.566] | REPORT_ONLY |
| 最初の 36 境界（生存本数を揃えた対照） | **-0.0489** [-0.0556, -0.0351] | -0.0279 [-0.0354, -0.0226] | **1.603** [1.391, 1.759] | LAG_IS_PROTECTIVE |

**最初の 150 境界は REPORT_ONLY**（窓依存の目視用）。$R' = |{\rm rate}_{\rm Aexact}| / |{\rm rate}_{\rm A1}|$ を seed ごとに 作り、seed クラスタ bootstrap（$B$=10,000・percentile・seed 20260829）で CI を出す。**生存本数を揃えても Aexact のほうが速く落ちる。**

## P1'-c 復活件数

| 腕 | 総数 | タスク内 | 境界 |
|---|---:|---:|---:|
| `L1w100_Aexact` | 0 | 0 | 0 |
| `L1w100_A1` | 34,222 | 25,723 | 8,277 |

10 seed 合計。`p_hat == 0` の記録点から次の記録点で `p_hat > 0` に戻った遷移を数える。

## P1'-d 全滅と unfit（REPORT_ONLY）

- `L1w100_Aexact` 全滅到達 task: **154–454（中央値 224）** / per-seed [188, 269, 271, 154, 238, 158, 318, 210, 196, 454]
- `L1w100_A1` は 5M までに全滅した seed なし
- `unfit` 最終値の中央値: `L1w100_Aexact` 1.0000 / `L1w100_A1` 0.0041

> **vault §1 の errata**: 全滅到達の中央値は「≈ 210」ではなく **224**（range は 154–454 で正しい）。210 は seed 7 の値である。

## Sanity

- **S_input**: PASS (gate)
- **S_mask**: PASS (gate)
- **S_reproduce**: PASS (gate)
- **S_known**: PASS (照合のみ)

S_reproduce は同じログから親の P1（`dbeta_boundary_499` の seed 中央値と $R_{499}$）を再計算して `results/center_oracle_0831/verdict.csv` と 1e-09 以内で一致することを確認する。**rate は同じマスクの厳密な細分である。**

## 引用上の注意

- **P1 の登録判定 `BOTH_CONTRIBUTE` を上書きしない。** 併記の形は「総和では `BOTH_CONTRIBUTE`（$R_{499}=0.5167$）だが、これは生存境界数 36 対 387 の打ち切りを含む。率に直すと符号が反転する（P1'）」
- **`R_{499}=0.5167` を単独で引かない。** 必ず率の行を併記する
- 可識別性の交絡は生きている。書いてよいのは「**EMA 遅れ窓を外すと悪化する**」までで、「$\mu$ が保護的である」ではない
- 「centering を改善すれば LoP を防げる」と書かない
- 0/10・10/10 は「観測しなかった」の強さ。`strict_dead = 1.0` が 10 seed で揃ったことは「必ず全滅する」ではない
